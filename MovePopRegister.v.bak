module MovePopRegister(moveIn,popTop,Write,Pop)
input Write;
input Pop;
input [2:0]moveIn;
output reg [2:0]popTop;
N=32*4;
reg [N:0]store;//stores 128/4 moves = 32 moves
always@(*)
case({Write,Pop})
	2'd0://wrie=0, pop=0;
	2'd1:popTop <= //write=0 pop=1;
	2'd2://write=1 pop=0;
	2'd3://write=1 pop =1;
endcase
end

endmodule

